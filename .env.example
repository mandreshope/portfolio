# GitHub Personal Access Token (optional – increases API rate limit from 60 to 5000/hr)
# Create one at: https://github.com/settings/tokens (no scopes needed for public repos)
# Then add it as a GitHub repo secret named VITE_GITHUB_TOKEN
# For local dev, create a .env.local file and add the line below (do not commit!)
VITE_GITHUB_TOKEN=
